apiVersion: v1
kind: ServiceAccount
metadata:
  name: aegis
  labels:
    app.kubernetes.io/name: aegis
# Aegis never talks to the Kubernetes API; pods also set
# automountServiceAccountToken: false so no token is mounted.
automountServiceAccountToken: false
